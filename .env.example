# ============================================
# ARTIST AUTOMATION SYSTEM - Environment Variables
# ============================================

# --------------------------------------------
# Memory Management (IMPORTANT!)
# --------------------------------------------
# Set to 'false' on low-RAM environments (O2Switch shared hosting)
# Set to 'true' on local/dev environments with sufficient RAM
ALLOW_HEAVY_SYNC=true

# --------------------------------------------
# Spotify API Credentials
# --------------------------------------------
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# --------------------------------------------
# AI Services
# --------------------------------------------
# Claude AI (for content generation)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Brave Search API (for web research)
BRAVE_SEARCH_API_KEY=your_brave_search_api_key_here

# --------------------------------------------
# WordPress Integration
# --------------------------------------------
# WordPress REST API endpoint
WORDPRESS_MCP_ENDPOINT=https://your-wordpress-site.com/wp-json

# WordPress Application Password (username:password format)
# Generate in WordPress: Users → Profile → Application Passwords
WORDPRESS_MCP_KEY=username:application_password_here

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port for the Express server
PORT=3000

# Subpath for deployment (e.g., /dancing-dead-relay-api for cPanel)
SUBPATH=/dancing-dead-relay-api

# --------------------------------------------
# GitHub Webhook (Automatic Deployment)
# --------------------------------------------
# Secret for GitHub webhook signature verification
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Configure this same secret in GitHub repository settings → Webhooks
WEBHOOK_SECRET=your_webhook_secret_here

# --------------------------------------------
# DEPLOYMENT NOTES
# --------------------------------------------
# LOCAL (Mac/Dev):
#   - ALLOW_HEAVY_SYNC=true (has enough RAM for syncs)
#   - Full artist sync via: POST /api/artists/sync
#
# PRODUCTION (O2Switch):
#   - ALLOW_HEAVY_SYNC=false (limited RAM, crashes on sync)
#   - Use: POST /api/artists/populate-queue (lightweight)
#   - Then run sync from local machine with more RAM
